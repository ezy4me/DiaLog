import { Box, ScrollView, VStack, Text } from "native-base";

const Page = () => {
  return <ScrollView>
    <VStack>
        <Box>
            <Text>Продукты</Text>
        </Box>
    </VStack>
  </ScrollView>;
};

export default Page;
